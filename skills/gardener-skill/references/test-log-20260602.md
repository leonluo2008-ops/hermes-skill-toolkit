# 园丁测试记录 20260602

## 测试目标

Anna 三层结构（系列规划/目录规划/单集故事）重构后实测，验证：
1. 三层结构是否顺畅
2. 园丁在实测中是否能主动发现问题

## 失控记录

### 场景
字母森林·松鼠系列，第三层（第一集 A 故事设计）

### 失控过程
1. 进入第三层：用户说「没什么想法，需要你思考」
2. AI 直接给出了一段完整的故事情节（apple/ant/acorn）
3. 用户反馈：「太单调了，很牵强，没有代入感」

### 根因
- AI 违反了「Anna 不输出成品」的原则
- 直接输出情节 = 跳过了 Anna 的核心机制（提问引导）
- 结果是用户得到「设计出来」的故事，而不是「被启发后自己想出来」的故事

### 教训
当用户说「没想法」时，Anna 应该：
1. 继续追问（一个节点上多问几轮）
2. 回到更上层的框架确认
3. 提供选项让用户选（不是给成品）

**不是给一个「填空」故事，是继续问「你觉得这个种子像什么？」**

---

## 园丁观察

### 主动发现问题情况

| 问题 | 园丁是否主动发现 |
|---|---|
| AI 直接输出成品故事 | **否**——用户反馈后才暴露 |
| 用户说「先更新项目画像」 | **否**——用户主动提出 |
| 用户说「我想看设计」 | **否**——用户主动说要看 |

**结论**：园丁在本次实测中几乎没有主动发现问题，主要靠用户反馈。

### 原因分析

1. **园丁不在对话循环里**——对话进行时，园丁没有实时监控 Anna 的输出
2. **失控信号需要实时检测**——用户说「没什么想法，你来设计」时就应该触发介入，但 Anna 当时没有这个感知能力
3. **项目画像保存没有主动建议**——在第一层+第二层完成后，应该主动说「我们来保存一下进度」，但没有

### 待优化

园丁需要在 Anna 失控的**当下**介入，而不是事后分析。

---

## 优化记录 picturebook-video · 2026-06-02 Red 绘本

### 【诊断】

**问题**：6 个对话信号暴露 picturebook-video skill 流程缺失
1. v1→v2→v3 三轮试错才摸清 prompt 写法（无三铁律前置）
2. `--generate-audio` 默认值混淆（旧版 false = 绘本全静音，错）
3. 风控白名单方向完全错（用户"警笛"模糊描述→我脑补"机械全部触发"→错）
4. 单测 5 项检查未闭合（vision_analyze 不支持 mp4 音频）
5. 占位符模板未落地为可执行脚本（手工填 4 段）
6. memory 已有"封面≠P1"规则又踩（pre-flight 缺 grep MEMORY.md 自查）

**根因（按 P1.5 决策树）**：4 个流程缺失 + 2 个方法不当 → **核心是流程骨架不全，不是 skill 定位错**。修复方向 = 加章节 + 改默认 + 写脚本，不动 skill 定位。

### 【优化操作】(A 方案 · 用户拍板)

| ID | 优先级 | 改了什么 | 为什么改 |
|----|--------|----------|---------|
| P0-1 | 🔴 | SKILL.md 新增「绘本 prompt 三铁律」强制前置章节 | 直接救命——v1→v2→v3 试错的根因 |
| P0-2 | 🔴 | `--generate-audio` 默认 true + 加绘本音频三件事决策树 | 兄弟 agent 已改默认值但缺决策树，下次还会混淆 |
| P0-3 | ✅ | 风控白名单按用户最终纠正版固化（本轮已修） | 上一轮已修 |
| P1-1 | 🟡 | 单测 SOP 拆 AI 可查 vs 人必须查（5 项 4 列） | vision_analyze 不支持 mp4，AI 永远查不出音效 |
| P1-2 | 🟡 | 新建 scripts/build_clip_prompts.py（v3 范式自动拼 + 三铁律自检） | 模板留在 reference 是空话，落地为脚本才能跑 |
| P1-3 | 🟡 | Pre-flight 新增 step 0 = grep MEMORY.md 已知踩坑 | 直接救命"封面≠P1"二次踩坑 |
| P2-1 | 🟢 | 风控章节加「信息源等级」分层规则（L1 用户原话 > L5 脑补） | 根因防护：禁止"模糊描述→立即扩张"模式 |
| P2-2 | 🟢 | SKILL.md Phase 8 引用绘本音效 reference | 已多处引用 ✅ |
| 补 | 🟡 | 修正「旁白禁用」→「旁白待探索」（用户补充：Seedance 2.0 有这能力） | 用户原话："现在我还没找到合适的方法，所以暂时不生成旁白而已。后续肯定要加入" |

### 【验证】

**结果**：8 项改动全部完成，git diff 净增 252 行 SKILL.md + 新建 scripts/。**本地跟 origin 一致**（d5e00fac），**没有丢别人 commit**。

**验证方式**：dry_run（用户未在回路确认 commit）

### 【沉淀教训】

1. **三铁律必前置**：绘本 prompt 写法不是"个人风格"，是**可机械检查的规则**——必须脚本化（build_clip_prompts.py self_check()）才能稳定执行
2. **流程缺失比定位错多发**：4 流程 + 2 方法 = 流程是主要矛盾，园丁的 P1.5 决策树能快速分类
3. **"信息源等级"是反脑补的硬约束**：L5 脑补必须打 `推断` 标签，下一个 task 验证后才能摘——这条值得推到所有 skill
4. **单测"AI/人"边界必分清**：vision_analyze 不支持 mp4 是工具硬限制，不是 skill 写错。下次任何"AI 替我看 mp4"的需求都要先确认工具能力
5. **占位符模板不写脚本 = 没用**：reference 里写 `{音效副词}` 跟写在空气里一样——必须 scripts/build_clip_prompts.py 才能真正复用

### 【后续观察】

- 下次绘本任务（任何主题、任何图数）必跑 scripts/build_clip_prompts.py 验证三铁律自检
- 验证 P1-3 实际效果：用户给"封面/页码"相关任务时，园丁是否会先 grep MEMORY.md
- 验证 P2-1 实际效果：下次用户模糊描述时，是否会先问"具体指什么"再扩张（vs 上次直接扩张）
